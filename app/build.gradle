dependencies {
  compile project(':common'), project(':api')
}

sourceSets {
    app {
        java {
            srcDir 'src/main/java'
        }
        resources {
            srcDir 'src/main/resources'
        }
    }
}