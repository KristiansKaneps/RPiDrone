allprojects {
  task hello << { task -> println "Name: $task.project.name" }
}

subprojects {
  apply plugin: 'java'

  sourceCompatibility = JavaVersion.VERSION_1_8
  version = '1.0'

  repositories {
    mavenCentral()
  }

  dependencies {

  }
}